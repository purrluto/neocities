// Made w/ jquery.
// No this isn't secure but it's okay because it's for a goofy little site.
// Don't put your passwords into JS...

$(document).ready(function () {
    let username = $('#user').val();
    let password = $('#pass').val();
});

$("form").submit(function(){
  alert("Submitted");
});